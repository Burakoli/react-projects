import React from "react";

const Form = () => {
    
    return(
        <form>
            <input type="text" placeholder="Add to-do..." />
            <button type="submit">Add to-do</button>
        </form>
    );
};

export default Form;